# Optional HTTP(S) endpoint for the external conversation and planning agent.
WHALEHALL_AGENT_API_URL=https://agent.example.com/

# Keep the real token only in your untracked .env file.
WHALEHALL_AGENT_API_TOKEN=replace-with-your-local-token
