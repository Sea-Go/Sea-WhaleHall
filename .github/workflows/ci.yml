name: CI

on:
  push:
    branches: [main]
  pull_request:
  merge_group:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: "1"
  RUST_TOOLCHAIN: 1.97.1
  BUN_VERSION: 1.3.14

permissions:
  contents: read

jobs:
  quality:
    name: Quality and complete test suite
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      WHALEHALL_CI_DISPLAY_MODE: degraded
      WHALEHALL_CI_FOREGROUND_MODE: degraded
      WHALEHALL_CI_PRESENCE_MODE: degraded

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Lint workflow definitions
        run: go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.12

      - name: Install workflow lint tools
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes shellcheck

      - name: Validate CI support scripts
        run: bash -n scripts/ci/*.sh

      - name: Run ShellCheck
        run: shellcheck scripts/ci/*.sh

      - name: Install Linux sensor dependencies
        run: scripts/ci/install-linux-dependencies.sh debian sensor

      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install Rust toolchain
        run: |
          rustup set profile minimal
          rustup toolchain install "$RUST_TOOLCHAIN" --component clippy --component rustfmt
          rustup default "$RUST_TOOLCHAIN"

      - name: Cache Rust build
        uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2
        with:
          workspaces: whalehall-local -> target
          cache-on-failure: true

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Report changed source lint
        if: github.event_name == 'pull_request' || github.event_name == 'merge_group'
        continue-on-error: true
        run: bun run lint:changed

      - name: Run complete checks
        run: bun run check

  hosted-sensors:
    name: Hosted sensor · ${{ matrix.name }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ubuntu-22.04
            os: ubuntu-22.04
            display_mode: degraded
            foreground_mode: degraded
            presence_mode: degraded
          - name: ubuntu-24.04
            os: ubuntu-24.04
            display_mode: degraded
            foreground_mode: degraded
            presence_mode: degraded
          - name: windows-server-2022
            os: windows-2022
            display_mode: required
            foreground_mode: required
            presence_mode: complete
          - name: windows-server-2025
            os: windows-2025
            display_mode: required
            foreground_mode: required
            presence_mode: complete
          - name: macos-14
            os: macos-14
            display_mode: required
            foreground_mode: required
            presence_mode: complete
          - name: macos-15
            os: macos-15
            display_mode: required
            foreground_mode: required
            presence_mode: complete
    runs-on: ${{ matrix.os }}
    env:
      WHALEHALL_CI_DISPLAY_MODE: ${{ matrix.display_mode }}
      WHALEHALL_CI_FOREGROUND_MODE: ${{ matrix.foreground_mode }}
      WHALEHALL_CI_PRESENCE_MODE: ${{ matrix.presence_mode }}

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Linux sensor dependencies
        if: runner.os == 'Linux'
        run: scripts/ci/install-linux-dependencies.sh debian sensor

      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install Rust toolchain
        shell: bash
        run: |
          rustup set profile minimal
          rustup toolchain install "$RUST_TOOLCHAIN"
          rustup default "$RUST_TOOLCHAIN"

      - name: Cache Rust build
        uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2
        with:
          workspaces: whalehall-local -> target
          cache-on-failure: true
          key: hosted-${{ matrix.name }}

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Test Rust workspace
        run: bun run test:rust

      - name: Test macOS release configuration
        if: runner.os == 'macOS'
        run: bun test tests/release-build-gate.test.ts

      - name: Open a deterministic Windows foreground application
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $probe = Start-Process notepad.exe -PassThru
          Start-Sleep -Seconds 2
          $activated = (New-Object -ComObject WScript.Shell).AppActivate($probe.Id)
          if (-not $activated) {
            throw "Unable to activate the Notepad sensor probe window."
          }

      - name: Open a deterministic macOS foreground application
        if: runner.os == 'macOS'
        run: |
          open -a TextEdit
          osascript -e 'tell application "TextEdit" to activate'
          sleep 2

      - name: Probe every sensor through the native process
        run: bun run test:sensors:ci

  linux-distros:
    name: Linux distro sensor · ${{ matrix.name }}
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    defaults:
      run:
        shell: /usr/bin/bash --noprofile --norc -e -o pipefail {0}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ubuntu-22.04
            image: ubuntu:22.04
            family: debian
          - name: ubuntu-24.04
            image: ubuntu:24.04
            family: debian
          - name: debian-12
            image: debian:12
            family: debian
          - name: debian-13
            image: debian:13
            family: debian
          - name: fedora-43
            image: fedora:43
            family: fedora
          - name: fedora-44
            image: fedora:44
            family: fedora
          - name: arch-rolling
            image: archlinux:base
            family: arch
          - name: rocky-9
            image: rockylinux:9
            family: rhel
          - name: alma-9
            image: almalinux:9
            family: rhel
          - name: centos-stream-9
            image: quay.io/centos/centos:stream9
            family: rhel
          - name: centos-stream-10
            image: quay.io/centos/centos:stream10
            family: rhel
          - name: opensuse-leap-16.0
            image: opensuse/leap:16.0
            family: suse
    container:
      image: ${{ matrix.image }}
    env:
      AR: /usr/bin/ar
      CC: /usr/bin/gcc
      CXX: /usr/bin/g++
      CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_LINKER: /usr/bin/gcc
      LANG: C.UTF-8
      PATH: /github/home/.bun/bin:/github/home/.cargo/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
      PKG_CONFIG: ${{ matrix.family == 'suse' && '/usr/bin/pkgconf' || '/usr/bin/pkg-config' }}
      WHALEHALL_CI_DISPLAY_MODE: degraded
      WHALEHALL_CI_FOREGROUND_MODE: degraded
      WHALEHALL_CI_PRESENCE_MODE: degraded

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install distribution dependencies
        run: scripts/ci/install-linux-dependencies.sh "${{ matrix.family }}" sensor

      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install Rust toolchain
        run: |
          if [[ -x /usr/bin/curl ]]; then
            /usr/bin/curl --proto '=https' --tlsv1.2 -fsSL \
              https://static.rust-lang.org/rustup/dist/x86_64-unknown-linux-gnu/rustup-init \
              -o /tmp/rustup-init
          else
            /usr/bin/wget --https-only --quiet \
              --output-document=/tmp/rustup-init \
              https://static.rust-lang.org/rustup/dist/x86_64-unknown-linux-gnu/rustup-init
          fi
          /usr/bin/chmod +x /tmp/rustup-init
          /tmp/rustup-init -y --profile minimal --default-toolchain "$RUST_TOOLCHAIN"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Test Rust workspace
        run: cargo test --locked --manifest-path whalehall-local/Cargo.toml --workspace

      - name: Probe every sensor in the distribution
        run: bun run test:sensors:ci

  virtual-x11:
    name: Linux virtual desktop · X11/Openbox
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      DISPLAY: :99
      WHALEHALL_CI_DISPLAY_MODE: required
      WHALEHALL_CI_FOREGROUND_MODE: required
      WHALEHALL_CI_PRESENCE_MODE: idle

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install X11 sensor dependencies
        run: scripts/ci/install-linux-dependencies.sh debian x11

      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install Rust toolchain
        run: |
          rustup set profile minimal
          rustup toolchain install "$RUST_TOOLCHAIN"
          rustup default "$RUST_TOOLCHAIN"

      - name: Cache Rust build
        uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2
        with:
          workspaces: whalehall-local -> target
          cache-on-failure: true
          key: virtual-x11

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Test Rust workspace
        run: bun run test:rust

      - name: Start a deterministic X11 desktop
        run: scripts/ci/start-x11-desktop.sh

      - name: Probe screen resolution and foreground usage
        run: bun run test:sensors:ci

  package:
    name: Package · ${{ matrix.name }}
    needs: [quality, hosted-sensors, linux-distros, virtual-x11]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macos-15
            os: macos-15
          - name: windows-2025
            os: windows-2025
          - name: ubuntu-24.04
            os: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Linux desktop build dependencies
        if: runner.os == 'Linux'
        run: scripts/ci/install-linux-dependencies.sh debian desktop

      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install Rust toolchain
        shell: bash
        run: |
          rustup set profile minimal
          rustup toolchain install "$RUST_TOOLCHAIN"
          rustup default "$RUST_TOOLCHAIN"

      - name: Cache Rust build
        uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2
        with:
          workspaces: whalehall-local -> target
          cache-on-failure: true
          key: package-${{ matrix.name }}

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build Electrobun canary
        run: bun run build:canary

      - name: Upload unsigned build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: whalehall-${{ matrix.name }}
          path: artifacts/**
          if-no-files-found: error
          retention-days: 7

  merge-gate:
    name: Merge gate
    needs: [quality, hosted-sensors, linux-distros, virtual-x11, package]
    if: ${{ always() }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Require every compatibility gate
        env:
          QUALITY_RESULT: ${{ needs.quality.result }}
          HOSTED_SENSORS_RESULT: ${{ needs.hosted-sensors.result }}
          LINUX_DISTROS_RESULT: ${{ needs.linux-distros.result }}
          VIRTUAL_X11_RESULT: ${{ needs.virtual-x11.result }}
          PACKAGE_RESULT: ${{ needs.package.result }}
        run: |
          for result in \
            "$QUALITY_RESULT" \
            "$HOSTED_SENSORS_RESULT" \
            "$LINUX_DISTROS_RESULT" \
            "$VIRTUAL_X11_RESULT" \
            "$PACKAGE_RESULT"; do
            if [[ "$result" != "success" ]]; then
              echo "::error::A required CI gate finished with result: $result"
              exit 1
            fi
          done
